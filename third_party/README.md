# Third-party source

`MNN/` is generated by `scripts/fetch_mnn.ps1` (Windows) or
`scripts/fetch_mnn.sh` (macOS/Linux) and is intentionally ignored by Git.

The scripts fetch MNN tag `3.5.0` and verify commit
`c35f14f3ab5cb65094863b9a0e888370b027a670` before the Android build uses it.
